/*
    JS data types are either primitive (value) or reference (object) types.

    - Primitive types are copied by value, so variables are independent.
    - Reference types are copied by reference, so multiple variables can point to the same object.
*/

// Primitive types
let stringType = "String"
let numberType = 92 // Includes floating points
let booleanType = false // Either true or false
let undefinedType // Default for uninitialized variables, can also be assigned explicitly
let nullType = null // Explicitly represents "no value"